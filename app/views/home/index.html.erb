<div class="bg-img">
	<div class="jumbotron">
		<div class="row">
      <div class="col-sm-10">

				<h1>Hello, Rails!</h1>
				<% if user_signed_in? %>
				  Logged in as <strong><%= current_user.email %></strong>.
				  <%= link_to 'Edit user details', edit_user_registration_path, :class => 'btn btn-info' %>
				  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-info'  %>
				<% else %>
				  <%= link_to "Sign up", new_user_registration_path, :class => 'btn btn-info'  %> |
				  <%= link_to "Login", new_user_session_path, :class => 'btn btn-info' %>
				<% end %>
      </div> 
			<div class="col-sm-2">
				<ul>
			    <li>
			      <%= link_to 'Supplier', suppliers_path %>
			    </li>
			     <li>
			      <%= link_to 'Patient', new_patient_path %>
			     </li>
			    <li>
			      <%= link_to 'Pysician', new_physician_path %>
			    </li>
			    <li>
			      <%= link_to 'Author', authors_path %>
			    </li>
			  </ul>
			</div>
		</div>
	</div>
</div>